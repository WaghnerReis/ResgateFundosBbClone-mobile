import styled from 'styled-components/native';

import {colors, metrics} from '../../../../styles';

export const Container = styled.View`
    height: 1px;

    background: ${colors.background};

    margin: ${metrics.paddingVertical} 0;
`;
